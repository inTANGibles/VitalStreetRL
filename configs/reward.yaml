# 奖励配置
reward:
  # 权重
  lambda_cost: 1.0  # 成本权重（每次动作扣除1，所以权重为1.0）
  mu_violation: 1.0  # 违规惩罚权重
  
  # 注意：动作成本固定为1.0（每次动作扣除1）
  # 成本计算在RewardCalculator._compute_cost中实现
  
  # 违规惩罚
  violation:
    min_street_width: 50.0  # 每违反一次
    protected_zone: 100.0  # 每违反一次
